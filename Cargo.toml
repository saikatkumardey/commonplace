[package]
name = "commonplace"
version = "0.1.0"
edition = "2021"
description = "Agent-agnostic long-term memory CLI with BM25 search"

[dependencies]
fastembed = "4"
rusqlite = { version = "0.31", features = ["bundled"] }

[profile.release]
strip = true
lto = true
